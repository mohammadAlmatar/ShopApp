
import 'package:flutter/material.dart';

class Productes extends StatelessWidget {
  const Productes ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Productes '),),
    );
  }
}
